//! TOML → JSON value translation.
//!
//! Stub for the `unified-config-config-crate` task — the actual
//! translation function lands there. Kept as a separate module so the
//! crate's public surface is stable from the schema-doc commit onward.

#![allow(dead_code)]
