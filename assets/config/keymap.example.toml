# This file is kept as a one-release-cycle redirect. The unified
# example config lives at `codon.example.toml` next to this one —
# it covers both [settings.*] and [bindings.*] sections in one place.
#
# Copy `codon.example.toml` to ~/.config/codon/codon.toml and edit.
# If you already have a ~/.config/codon/keymap.toml on disk, codon's
# first-launch migration will fold it into codon.toml automatically;
# nothing manual to do.
